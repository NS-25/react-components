import React from "react";
import { useState } from "react";

const GenderGroup = ({ onUpdateGender, label, select, value }) => {
  const [selectedGender, setSelectedGender] = useState()
  return <div></div>;
};

export default GenderGroup;
